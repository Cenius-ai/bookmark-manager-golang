# Copy this file to `.env` and fill in the real values.
# `.env` is gitignored and should NEVER be committed.
# Values here are placeholders / examples ONLY.

# ─── Required ───

# Provider: Database
# Docs: Connection string for your database; format: <scheme>://<user>:<password>@<host>:<port>/<dbname>
# dev-only; regenerate for production. Database configuration. Obtain from the provider dashboard.
DATABASE_URL=cenius-dev-8a32985f554e82895868484845168d4b

# ─── Optional ───

# App configuration value.
FONTS_DIR=./data

# App configuration value.
FONT_CSS=<set in .env>

# App configuration value.
FONT_CSS_URL=http://localhost:8080

# App configuration value.
PATH=<set in .env>

# Provider: Runtime config
# Docs: Standard runtime configuration; not secret
# Runtime config configuration. Obtain from the provider dashboard.
PORT=8080
